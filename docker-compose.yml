services:
  smart_llm_gateway:
    build:
      context: .
      args:
        APP_PORT: ${PORT:-8501}
    container_name: smart_llm_gateway-app
    restart: unless-stopped
    environment:
      PORT: ${PORT:-8501}
    ports:
      - "127.0.0.1:${HOST_PORT:-8501}:${PORT:-8501}"
