# Values are injected from AnyCloud's session-only secret dialog at deployment time.
# This template deliberately contains no credential values.
apiVersion: v1
kind: Secret
metadata:
  name: smart-llm-gateway-api-keys
type: Opaque
stringData:
  EXAMPLE_API_KEY: "[REMOVED-BY-ANYCLOUD-SECRET-GUARD]"
