apiVersion: v1
kind: Service
metadata:
  name: smart-llm-gateway
  labels:
    app.kubernetes.io/name: smart-llm-gateway
    app.kubernetes.io/managed-by: anycloud-studio
spec:
  type: LoadBalancer
  selector:
    app: smart-llm-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
